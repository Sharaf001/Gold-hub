# Copy this file to .env
# Where the frontend should send API requests. When running the API
# server separately in dev (its own port, e.g. 5000), point this at it.
VITE_API_BASE_URL=http://localhost:5000/api
